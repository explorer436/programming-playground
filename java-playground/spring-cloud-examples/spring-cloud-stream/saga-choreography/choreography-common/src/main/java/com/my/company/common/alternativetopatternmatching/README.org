This is an alternate approach to using "pattern matching" as shown in InventoryEventProcessor, OrderEventProcessor, PaymentEventProcessor and ShippingEventProcessor.
"pattern matching" is an experimental feature in jdk17. It is a full-fledged feature since jdk21.
The other alternative is to use if-else statements.